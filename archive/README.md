# Archive

历史内容只读保留，用于追溯，不参与当前状态计算：

- `early-search/`：早期路线与证伪历史；
- `prompts/`：旧服务器执行 prompt；
- `superseded/`：被后续裁决替代的计划/分析；
- `legacy-layout/`：迁移前模块入口说明；
- `research-history/`：后续可接收阶段性旅程与上下文快照。

当前入口始终是仓库根 README 和 `decisions/CURRENT.md`。
