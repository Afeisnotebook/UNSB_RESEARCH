# HNEK lineage

`time-active/path-consistency failures → return to bridge-native endpoint kernel → physical remaining horizon → normalized residual coordinates → bounded gamma/coordinate search → gamma=0.25 development freeze`。
